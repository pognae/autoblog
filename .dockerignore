node_modules
**/node_modules
dist
**/dist
*.log

# 글감(posts)·DB는 이미지에 포함. app-config.json(API키·봇토큰)은 제외 → 환경변수/볼륨으로 주입
server/.session
server/screenshots
server/data/uploads
server/data/app-config.json

# 환경변수 파일은 이미지에 굽지 않는다 (배포 플랫폼의 환경변수로 주입)
.env
**/.env
!**/.env.example

.git
.gitignore
*.md
