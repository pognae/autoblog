node_modules
**/node_modules
dist
**/dist
*.log

# 글감(posts)·DB·설정파일(app-config.json)은 이미지에 포함해 함께 배포.
# (민감정보는 .env 전용이라 이 파일들엔 키/토큰이 없음). 세션/스크린샷/임시업로드만 제외.
server/.session
server/screenshots
server/data/uploads

# 환경변수 파일은 이미지에 굽지 않는다 (배포 플랫폼의 환경변수로 주입)
.env
**/.env
!**/.env.example

.git
.gitignore
*.md
